# DONE: Add routes as follows:
#
GET /time                controllers.TimeController.time
# This has to appear before the next one
GET /time/zones          controllers.TimeController.zones
GET /time/:zoneId        controllers.TimeController.timeIn(zoneId: String)
